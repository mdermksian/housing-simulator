import { GroupBox, VerticalBox } from "std-widgets.slint";

export component ConfigurationSummary inherits GroupBox {
    in property <string> summary;
    title: "Preview";

    VerticalBox {
        Text {
            text: root.summary;
            wrap: word-wrap;
        }
    }
}
